parallel {
  stage('Build') { 
    stages { 
      stage ('template_a') 
      stage ('deploy_a') { 
    }
  stage('b') {
    stages {
      stage ('template_b') 
      stage ('deploy_b') 
    }
  }
}
